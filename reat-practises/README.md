# notes

## Hooks
<!-- lifecycle methodsclass methods kemliye use hota h and the reason for generation of hooks
lifecyycle me are mounting, updating and unmounting
1.hooks are functions 
2.hooks allow to use state and other features without writing a class
-->

rules to use hooks
1. only call hooks at the top level
2. called only from react functions

react componentet cannot re-render the normal defined variables